# gforward 项目说明

## 1. 项目定位

`gforward` 是一个用 Go 实现的 TCP 正向代理/转发隧道。程序分为客户端和服务端两个角色：

- 客户端监听本地流量，从 HTTP Host、TLS SNI、HTTP Proxy 请求或 SOCKS5 请求中识别目标地址。
- 客户端通过自定义二进制协议，将目标地址和用户数据发送给 gforward 服务端。
- 服务端连接真实目标站点，双向转发客户端与目标站点之间的 TCP 字节流。

模块路径为 `github.com/near-notfaraway/gforward`，命令名和构建产物名为 `gforward`。项目使用 GPL-3.0 许可证。

## 2. 已实现功能

### 2.1 客户端接入模式

| 模式 | 默认/固定端口 | 目标地址来源 | 行为 |
| --- | ---: | --- | --- |
| `http_proxy` | 8080 | HTTP 请求的 Host | 支持普通 HTTP 代理请求；支持 CONNECT 隧道并返回 `200 OK` |
| `socks5` | 1080 | SOCKS5 CONNECT 请求地址 | 仅支持无认证和 CONNECT 命令 |
| `http_dns` | 固定 80 | HTTP 请求的 Host | 启动 DNS 服务，将 A 查询劫持到本机后接收透明 HTTP TCP 流量 |
| `https_dns` | 固定 443 | TLS ClientHello 的 SNI | 启动 DNS 服务，将 A 查询劫持到本机后接收透明 HTTPS TCP 流量 |

`http_dns` 和 `https_dns` 启动时还会监听 UDP 53，并将任意 IN A 查询响应为 client 的请求侧网卡 IPv4。两种模式的监听端口必须分别为 TCP 80/443，并从应用层协议中推断真实目标站点。DNS 协议不能指定 TCP 端口，因此客户端流量依靠 HTTP/HTTPS 默认端口进入对应监听器。

### 2.2 服务端能力

- 默认监听 TCP `9989`。
- 解析客户端发送的 ForwardPacket。
- 为每条客户端连接建立对应的目标 TCP 连接，连接超时为 2 秒。
- 将请求 payload 写入目标站点，将目标站点响应原样写回客户端。
- 多核模式下按客户端连接字符串的 FNV-1a 哈希固定分配到一个消息处理器，避免同连接跨处理器访问普通 map。

### 2.3 运维能力

- `--multicore` 控制 gnet 多核事件循环，默认开启。
- `--verbose` 将日志级别从 `warn` 调整为 `debug`。
- 日志基于 logrus；命令行当前输出到 stdout。
- `build.sh` 支持本机、`darwin/amd64`、`linux/amd64` 静态构建，并生成 `output/gforward.tgz`。

## 3. 总体架构

```text
应用/浏览器
    |
    | HTTP / HTTPS / HTTP Proxy / SOCKS5
    v
client.ListenHandler (gnet server)
    |
    | 解析目标地址，维护 userConn <-> serverConn
    | ForwardPacket: destination + payload
    v
dialer.Dialer (gnet client)
    |
    | TCP
    v
server.Dispatcher (gnet server)
    |
    | 按连接哈希分发
    v
server.MsgHandler
    |
    | 维护 clientConn <-> destConn
    v
dialer.Dialer (gnet client) ---> 真实目标站点

响应路径反向返回，响应数据使用 PlainPacket，即原始 TCP 字节。
```

核心设计是“一条用户连接对应一条客户端到服务端连接，并在服务端同一时刻对应一条目标站点连接”。普通 HTTP 代理按请求解析目标，Host 改变时关闭旧目标连接并建立新连接；CONNECT、SOCKS5 和透明代理连接的目标保持固定。

## 4. 核心数据流

### 4.1 上行：用户到目标站点

1. `cmd/client.go` 根据 `--mode` 确定监听端口和目标解析器。
2. `client.ListenHandler.OnTraffic` 首次收到连接数据时调用 `destination.Parser`。
3. 解析器返回三态结果：`ParseNeedMoreData` 保留未完成数据并等待下一次事件，`ParseDone` 给出 `host:port`，`ParseRejected` 关闭连接。普通 HTTP 代理还会返回当前请求的字节边界。
4. 客户端为该用户连接建立到 gforward 服务端的 TCP 连接。
5. 客户端把目标地址和最多 65535 字节 payload 编码为 ForwardPacket。
6. `server.Dispatcher.OnTraffic` 解码一个完整 ForwardPacket，并按连接哈希分发。
7. `server.MsgHandler` 首次收到该连接的数据时拨号目标站点，随后写入 payload。

### 4.2 下行：目标站点到用户

1. 服务端 Dialer 收到目标站点字节流，以 PlainPacket 形式投递给对应 `MsgHandler`。
2. `MsgHandler` 根据 `destConn -> clientConn` 映射，将原始 payload 写回客户端连接。
3. 客户端 Dialer 收到响应后，根据 `serverConn -> userConn` 映射写回本地用户连接。

## 5. 私有协议

`protocol.InternalPacket` 抽象了 `ForwardPacket` 和 `PlainPacket`。

ForwardPacket 使用大端序：

```text
+----------------------+----------------------+------------------+------------------+
| destination_len: u16 | destination: N bytes | payload_len: u16 | payload: M bytes |
+----------------------+----------------------+------------------+------------------+
```

- `destination` 是目标 `host:port` 字符串。
- 单帧 payload 长度由 `uint16` 表示，客户端每次最多发送 65535 字节。
- 上行使用 ForwardPacket；下行 PlainPacket 没有额外帧头，直接承载当前收到的原始字节。
- 协议没有版本号、认证、加密、压缩或完整性校验。

## 6. 目录与职责

| 路径 | 职责 |
| --- | --- |
| `main.go` | 程序入口，执行 Cobra 根命令 |
| `cmd/` | `client`、`server` 子命令，参数、日志和 gnet 启动配置 |
| `client/common.go` | 客户端监听、连接映射、封包和双向转发 |
| `client/destination/` | HTTP Host、TLS SNI、HTTP Proxy、SOCKS5 目标解析 |
| `client/dns.go` | DNS A 记录劫持处理器及 UDP DNS 服务 |
| `server/dispatcher.go` | 服务端收包和按连接哈希分发 |
| `server/handler.go` | 目标连接管理及双向数据转发 |
| `dialer/` | 基于 gnet.Client 的异步拨号和收包适配层 |
| `protocol/` | 内部包接口、ForwardPacket、PlainPacket |
| `diagnosis/` | logrus 初始化和可选滚动文件输出 |
| `utils/` | 连接格式化和 FNV-1a 字符串哈希 |
| `build.sh` | 编译和归档脚本 |

## 7. 启动与构建

```bash
# 启动服务端
go run . server --port 9989

# 启动 HTTP 代理客户端
go run . client --mode http_proxy --listen 127.0.0.1:8080 --server 127.0.0.1:9989

# 启动供局域网设备使用的透明 HTTP 客户端与 DNS 服务
go run . client --mode http_dns --listen 192.168.1.10:80 --server 127.0.0.1:9989

# 启动 SOCKS5 客户端
go run . client --mode socks5 --listen 127.0.0.1:1080 --server 127.0.0.1:9989

# 输出调试日志
go run . client --mode http_proxy --listen 127.0.0.1:8080 --server 127.0.0.1:9989 --verbose

# 本机构建并打包
bash build.sh

# 指定 amd64 目标平台
SYSTEM=darwin bash build.sh
SYSTEM=linux bash build.sh
```

客户端各模式默认监听 `0.0.0.0:<模式默认端口>`，也可由 `--listen IPv4:port` 覆盖；服务端使用 `tcp://:<port>` 监听所有可用网络接口。

## 8. 关键依赖

- Go 1.22
- Cobra：命令行框架
- gnet v2：客户端和服务端事件驱动网络框架
- logrus：结构化日志
- txthinking/socks5：SOCKS5 报文解析和响应
- miekg/dns：提供 UDP DNS 服务和 DNS 消息编解码
- go-file-rotatelogs：滚动日志文件支持

## 9. 当前边界与注意事项

- 项目没有配置文件，所有运行参数来自命令行。
- `--listen` 和 `--server` 都只接受 `IPv4:port`；DNS 模式使用具体 listener IP，或在监听 `0.0.0.0` 时按请求路由选择本地网卡 IP，作为所有 A 记录的响应。
- `http_dns` 的 listener 端口必须为 80，`https_dns` 的 listener 端口必须为 443。
- DNS 服务仅伪造 IPv4 A 记录，不响应 AAAA 等其他记录类型。
- 默认 DNS 端口 53 以及透明代理端口 80/443 在部分系统上需要管理员权限。
- `http_dns` 的 HTTP 解析器直接返回 Host；Host 未显式携带端口时不会自动补 `:80`。
- TLS 目标识别依赖 ClientHello 中存在 SNI，不支持无 SNI 流量。
- SOCKS5 仅支持无认证 CONNECT，不支持 BIND、UDP ASSOCIATE 或用户名密码认证。
- 普通 HTTP 代理支持同一 TCP 连接顺序访问不同 Host，不支持 HTTP/1.1 pipelining 的并发响应排序。
- 自定义协议运行在明文 TCP 上，服务端没有身份认证、访问控制和目标地址白名单。监听公网时会形成安全风险。
- ForwardPacket 支持等待半包和循环拆包，但反序列化错误尚未区分“数据不足”和“非法帧”，非法帧可能长期滞留缓冲区。
- 客户端关闭用户连接时会删除映射，但不会主动关闭对应的服务端连接；服务端会清理映射并关闭对应目标连接。
- `build.sh` 会先删除整个 `output` 目录，且固定交叉编译目标为 amd64。

## 10. 修改指引

- 结构体定义后连续放置其构造函数和方法组，不要在结构体与其方法之间穿插其他结构体定义，保证代码审查时可连续阅读。
- 定义结构体时必须为每个字段添加简短注释，说明该字段的用途或所维护的状态。
- 函数体超过 20 行时，必须在函数定义前添加一行简短注释，说明该函数的主要职责。
- UT 单测统一基于 GoConvey 和 Mockey 实现。
- 每个被测函数尽量由一个顶层 UT 函数覆盖，不同场景通过多层嵌套 `Convey` 或 `PatchConvey` 组织。
- 新增接入协议：在 `client/destination` 实现 `Parser`，并在 `NewParser` 和 `cmd/client.go` 注册模式。短包必须返回 `ParseNeedMoreData`，协议错误返回 `ParseRejected`，只有目标地址完整时才能返回 `ParseDone`。
- 修改隧道帧格式：同步修改 `protocol.ForwardPacket` 的 Marshal/Unmarshal，并重点验证 TCP 半包、粘包和最大长度。
- 修改连接生命周期：同时检查客户端的三组连接映射和服务端的两组连接映射，确保双向关闭与清理对称。
- 修改并发模型：保持同一服务端入站连接固定落到同一个 `MsgHandler`，否则 handler 内普通 map 会产生并发访问。
- 修改代理握手：只消费已经完整解析的握手字节，保留不完整报文和粘在握手后的业务字节，避免重复发送或误丢弃首包。
